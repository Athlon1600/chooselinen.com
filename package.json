{
  "name": "chooselinen",
  "description": "Source code for chooselinen.com",
  "version": "0.0.1",
  "scripts": {
    "format": "prettier --write public",
    "serve": "live-server --cors ./public",
    "deploy:prod": "npx netlify deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod --dir=./public"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.6.2",
    "prettier-plugin-css-order": "^2.1.2"
  }
}
